<div class="md:flex justify-between items-center pb-4">
  <h1>Totals by Programs Report</h1>
  <%= link_to 'Back to Reports', system_reports_path, class: "primary_button" %>
</div>
<p>This report shows totals by programs statistics.</p>

<div class="py-3">
  <%= render 'filter_form', report_url: totals_programs_report_system_reports_path %>

  <% if params[:commit] && params[:format] == 'html' %>
    <%= render 'reports_table' %>
  <% end %>
</div>
