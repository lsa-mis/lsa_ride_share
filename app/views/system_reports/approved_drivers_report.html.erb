<div class="md:flex justify-between items-center pb-4">
  <h1>Approved Drivers Report</h1>
  <%= link_to 'Back to Reports', system_reports_path, class: "primary_button" %>
</div>
<p>This report shows all approved drivers for selected term and unit.</p>

<div class="py-3">
  <%= render 'filter_form', report_url: approved_drivers_report_system_reports_path %>

  <% if params[:commit] && params[:format] == 'html' %>
    <%= render 'reports_table' %>
  <% end %>
</div>
