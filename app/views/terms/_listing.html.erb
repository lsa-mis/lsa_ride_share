<%= turbo_frame_tag 'terms' do %>
  <div class="min-w-full">
    <table class="mi_table">
      <thead class="border border-l-0 border-r-0 border-gray-um60">
        <tr>
          <th class="header_th">Name</th>
          <th class="header_th">Code</th>
          <th class="header_th">Start Date</th>
          <th class="header_th">End Date</th>
        </tr>
      </thead>
      <tbody>
        <% @terms.each do |term| %>
          <tr class="mi_tbody_tr">
            <td class="mi_tbody_td">
              <%= link_to term.name, term_path(term), class: "link_to" %>
            </td>
            <td class="mi_tbody_td">
              <%= term.code %>
            </td>
            <td class="mi_tbody_td">
              <%= show_date(term.term_start) %>
            </td>
            <td class="mi_tbody_td">
              <%= show_date(term.term_end) %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>
