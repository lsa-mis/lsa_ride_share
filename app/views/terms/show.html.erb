<div class="mx-auto w-full">
  <div class="md:flex justify-between items-center pb-4">
    <h1>Term Name: <%= @term.name %></h1>
    <div class="py-2">
      <%= link_to 'Edit Term', edit_term_path(@term), class: "primary_button" %>
    </div>
  </div>
  <div class="grid grid-cols-1 lg:grid-cols-2">
    <%= render @term %>
  </div>
</div>
