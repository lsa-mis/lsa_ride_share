json.extract! program, :id, :active, :title, :term_start, :term_end, :term_id, :subject, :catalog_number, :class_section, :number_of_students, :number_of_students_using_ride_share, :pictures_required_start, :pictures_required_end, :non_uofm_passengers, :created_at, :updated_at
json.url program_url(program, format: :json)
