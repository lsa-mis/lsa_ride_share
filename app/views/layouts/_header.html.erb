<nav class="bg-blue-900">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="flex h-16 items-center justify-between">
      <div class="flex items-center">
        <div class="flex-shrink-0">
          <%= link_to root_path do %>
            <div class="flex items-center">
              <div class="inline-block h-8 justify-self-start content-start items-start">
                <%= image_tag 'mlsa.png', alt: 'LSA Logo', height: '35.75', width: '100', class: '' %>
              </div>
              <span class="text-white navlogo-sm-text xl:navlogo-text pl-4">
                Ride Share
              </span>
            </div>
          <% end %>
        </div>
        <div class="hidden lg:ml-6 lg:block">
          <div class="flex space-x-4">
            <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
            <a href="#" class="rounded-md px-3 py-2 nav-sm-text xl:nav-text text-yellow-ummaze">Programs</a>
            <a href="#" class="rounded-md px-3 py-2 nav-sm-text xl:nav-text text-gray-300 hover:bg-gray-700 hover:text-white">Reservations</a>
            <a href="#" class="rounded-md px-3 py-2 nav-sm-text xl:nav-text text-gray-300 hover:bg-gray-700 hover:text-white">Unit Options</a>
          </div>
        </div>
      </div>
      <div class="hidden lg:ml-6 lg:block">
        <div class="flex items-center">
          <!-- Profile dropdown -->

            <% if user_signed_in? %>
              <div>
                <div class="text-white body-text pb-1">Welcome <%= current_user.display_name.split[0] %></div>
                <%= button_to destroy_user_session_path, method: :delete, data: {turbo: "false"} do %>
                  <span class="sign_button body-md-text">Sign Out</span>
                <% end %>
              </div>
            <% else %> 
              <div>
                <div class="text-white body-text pb-1">&nbsp;</div>
                <%= button_to user_saml_omniauth_authorize_path, data: {turbo: "false"} do %>
                  <div class="sign_button body-md-text">Sign In</div>
                <% end %>
              </div>
            <% end %>

        </div>
      </div>
      <div class="-mr-2 flex lg:hidden">
        <!-- Mobile menu button -->
        <button type="button" class="relative inline-flex items-center justify-center rounded-md p-2 text-gray-400 hover:bg-gray-700 hover:text-white focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" aria-controls="mobile-menu" aria-expanded="false">
          <span class="absolute -inset-0.5"></span>
          <span class="sr-only">Open main menu</span>
          <!--
            Icon when menu is closed.

            Menu open: "hidden", Menu closed: "block"
          -->
          <svg class="block h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
          </svg>
          <!--
            Icon when menu is open.

            Menu open: "block", Menu closed: "hidden"
          -->
          <svg class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu, show/hide based on menu state. -->
  <div class="lg:hidden" id="mobile-menu">
    <div class="space-y-1 px-2 pb-3 pt-2">
      <!-- Current: "bg-gray-900 text-white", Default: "text-gray-300 hover:bg-gray-700 hover:text-white" -->
      <a href="#" class="block rounded-md px-3 py-2 nav-md-text text-yellow-ummaze">Programs</a>
      <a href="#" class="block rounded-md px-3 py-2 nav-md-text text-gray-300 hover:bg-gray-700 hover:text-white">Reservations</a>
      <a href="#" class="block rounded-md px-3 py-2 nav-md-text text-gray-300 hover:bg-gray-700 hover:text-white">Unit Options</a>
    </div>
    <div class="border-t border-gray-700 pb-3 pt-4">
      <div class="mt-3 space-y-1 px-2">
        <div>
          <% if user_signed_in? %>
            <div class="text-white body-text pb-1">Welcome <%= current_user.display_name %></div>
            <%= button_to destroy_user_session_path, method: :delete, data: {turbo: "false"} do %>
              <div class="sign_button">Sign Out</div>
            <% end %>
          <% else %> 
            <div>
              <%= button_to user_saml_omniauth_authorize_path, data: {turbo: "false"} do %>
                <div class="sign_button">Sign In</div>
              <% end %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
