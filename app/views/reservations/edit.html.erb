<div class="mx-auto w-full">
  <h1>Editing reservation</h1>
  <%= render "edit_form", reservation: @reservation %>
  <div>
    <%= turbo_frame_tag "available_cars" do %>
      <%= render "available_cars_edit" %>
    <% end %>
  </div>
</div>
